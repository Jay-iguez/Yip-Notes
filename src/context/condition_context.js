import { createContext } from "react";

// Starts our condition_context on the screen 'kennels-list as a default

const condition_context = createContext('kennels-list')

export default condition_context