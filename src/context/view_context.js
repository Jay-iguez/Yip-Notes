import { createContext} from "react";

// context to set the entire screen of a given kennel based on a capped yips size of that kennel

const view_context = createContext(false)

export default view_context
